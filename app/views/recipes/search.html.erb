<% if recipes.blank? %>
  <H1>No recipes found</H1>
<% else %>
  <% recipes.each do |recipe| %>
    <%= render "recipe_row", recipe: recipe %>
  <% end %>
<% end %>

<%= link_to 'Back', recipes_path %>
